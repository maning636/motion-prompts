# 书页壳（纸感页面展示框）

> 分类：杂志拼贴 ｜ 时长：6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】书页壳（纸感页面展示框）
【分类】杂志拼贴
【用途】纸白细网格底 + 顶部细线页眉（VOL/页码），左栏 108px 大标题（第二行荧光绿高亮扫入），右侧 600×800 书页卡（墨黑细边 + 硬偏移投影）rotateX 微倾斜落位、绿扫光掠过，底部荧光绿署名条。图片/视频双槽择一，可把任意已渲染片段套进纸感书页。
【标签】书页 / 纸感 / 页面展示 / 图片槽
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
纸白 #F6F5EF + 36px 细网格底；顶部细线页眉（左 VOL/栏目 + 右页码 mono），左栏 108px 大标题两行（第二行荧光绿高亮块 scaleX 扫入），右侧 600×800 书页卡（3px 墨黑边 + 16px 硬偏移投影）y+150/rotateX 16° expo.out 落位，随后绿色扫光掠过页面，底部荧光绿署名条（@老马AI研习社）推入，收尾书页 ±8px 呼吸浮动。图片/视频双槽择一：video1 填路径播视频，否则显示 img1。
【可改文案】页眉左（卷/栏目）（默认「VOL.15 · 视觉动效库」）、页眉右（页码）（默认「P.075 / 400」）、眉题（默认「PAGE SHELL · 书页壳」）、大标题第一行（默认「把任何一页」）、大标题第二行（绿高亮）（默认「放进书页里」）、说明行（默认「图片或已渲染的视频片段，统一套进纸感」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
