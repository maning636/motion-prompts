# 挤出环形爆炸饼图（模式特效）

> 分类：数据可视化 ｜ 时长：6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】挤出环形爆炸饼图（模式特效）
【分类】数据可视化
【用途】2.5D 伪 3D 挤出环形饼图（纯 SVG，不引 three.js）：圆盘旋入→扇区顺时针逐块弹起向外分离→引导线+百分比延迟浮出，θ=value/total×360°，空心环形+金属外圈+挤出厚度侧壁。
【标签】饼图 / 环形图 / 2.5D / 挤出
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
深色底 #080A08。SVG viewBox 860：底座整环下移 26px 深色金属体充挤出厚度，每扇区外侧壁 path（shade .22）+顶面（accent 明度梯度 1.0/.55/.8/.4…），金属外圈 linearGradient 灰白渐变 stroke。旋入 rotation -150→0 scale .55→1 power3.out 1.1s；扇区沿角平分线外移 34px+上抬 10px back.out(1.8) 顺时针 stagger .16；引导线 polyline + 标签 + accent 百分比 2.15s 起 stagger .12 浮出。定格末帧。
【可改文案】标题（默认「内容形态占比」）、数据数组（逗号分隔）（默认「42,28,18,12」）、标签（逗号分隔）（默认「短视频,图文,直播,其他」）、强调色（默认「#00FF00」）、总时长(秒)（默认「6」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
