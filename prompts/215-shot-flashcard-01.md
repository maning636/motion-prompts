# 底色闪砸字

> 分类：镜头动效 ｜ 时长：4.5s ｜ 在线预览：https://maning789link.top/station.html

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】底色闪砸字
【分类】镜头动效
【用途】大词逐拍歪着图章砸屏，落定瞬间背景两纯色频闪数帧而文字纹丝不动，主体稳背景闪的动漫必杀技字卡
【标签】砸字 / 口号 / 节拍
【画幅】1920×1080（16:9 横屏）
【时长】4.5 秒，动画确定性、首尾可循环

【视觉与设计规范】
大词逐拍歪着图章砸屏，落定瞬间背景两纯色频闪数帧而文字纹丝不动，主体稳背景闪的动漫必杀技字卡。
【可改文案】词1（默认「日更」）、词2（默认「不难」）、词3（默认「现在就开始」）、标签条（默认「模板库 · 215 个镜头直接抄」）、强调色（默认「#00FF00」）、背景色（默认「#0A0A0F」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
