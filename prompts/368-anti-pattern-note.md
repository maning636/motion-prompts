# 反例警示双卡（教学外壳）

> 分类：字幕与重点标注 ｜ 时长：6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】反例警示双卡（教学外壳）
【分类】字幕与重点标注
【用途】反例警示对照：✗错误示范 vs ✓正确做法双卡左右撞入（红✗/绿✓圆形印章 back.out 盖章），顶部图槽可放对照截图，踩坑/正解类教学段落直接用。
【标签】教学外壳 / 反例 / 对照 / 警示
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
深色规范风。双卡 830×660：错误侧 #171110 + #FF5147 描边/顶条/✗印章，正确侧 #0F1710 + accent 描边/顶条/✓印章；顶部 250px 图槽条（image×2，占位图兜底画面仍完整），印章盖在图右上。卡从 ±80px 撞入，顶条 scaleX 展开，印章 scale 0→1 带 ±18° 回旋 back.out(2.2) 盖章。定格双卡对照。
【可改文案】标题（默认「同一个坑，两种写法」）、错误文案（默认「让两圈花瓣各自错开旋转——蒙版接缝直」）、正确文案（默认「内外两圈同角同速，一次转完——窗口扫」）、错误示范图（可选）（默认「./placeholder-1.pn」）、正确做法图（可选）（默认「./placeholder-2.pn」）、正确侧强调色（默认「#00FF00」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
