# 里程表数字滚动

> 分类：镜头动效 ｜ 时长：6s ｜ 在线预览：https://maning789link.top/station.html

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】里程表数字滚动
【分类】镜头动效
【用途】巨型指标铺满画面，每一位数字各自竖向滚动并拖出残影，自左向右依次回弹到位——王牌数据的高光时刻。
【标签】数字 / 数据 / 滚动 / 指标
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
全屏巨号指标每个数位独立纵向滚动带残影，从左到右逐位过冲锁定，适合王牌数据亮相。
【可改文案】数值（默认「99.98」）、单位（默认「%」）、指标名称（默认「好评率」）、结论文案（默认「连续 12 个月保持」）、强调色（默认「#00FF00」）、背景色（默认「#0A0A0F」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
