# 霓虹代码窗口卡

> 分类：信息卡片 ｜ 时长：6s ｜ 在线预览：https://maning789link.top/station.html

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】霓虹代码窗口卡
【分类】信息卡片
【用途】深黑底荧光绿左文右窗：标题关键词绿色高亮，右侧 macOS 代码窗口内 5 行伪代码逐行 cascade 淡入，适合口播讲生成流程/构建进度。
【标签】代码窗口 / 霓虹 / 荧光绿 / 生成流程
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
按 TEMPLATE-INGEST-STANDARD §2.5 规范：时长 duration 变量化，动效演完定格最后一帧。
【可改文案】英文小标签（全大写）（默认「BUILD PROGRESS」）、中文标题（含高亮词）（默认「它会一步一步问你」）、标题高亮词（绿色，须为标题子串）（默认「一步一步」）、代码窗口名（默认「generator.js」）、代码行1（默认「const profile = aw」）、代码行2（默认「const style = awai」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
