# 图层爆炸分解图（教学外壳）

> 分类：产品演示 ｜ 时长：6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】图层爆炸分解图（教学外壳）
【分类】产品演示
【用途】N 层卡片 3D 等轴倾斜爆炸分解：堆叠状态逐层沿 Z 轴错位展开（expo.out stagger），右侧层号清单同步列出，中间层可嵌图（image 槽）。讲结构/架构分层用。
【标签】教学外壳 / 图层 / 爆炸分解 / 3D 倾斜
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
深色规范风。stage perspective 1700px，tilt 容器 rotateX(56deg) rotateZ(-34deg) 等轴角（入场动效作用在外层 stage 避免覆盖 transform）；层卡 680×420 rgba(18,22,19,.94)+accent 描边，初始 z 间隔 9px，0.75s 起 stagger .12 expo.out 炸到 z 间隔 66px；中间层嵌 image 槽图；右侧 L0N→L01 层号清单 accent 序号 stagger 列出。定格爆炸态。
【可改文案】标题（默认「五层结构，一次看清」）、层名（逗号分隔，自下而上）（默认「背景 BG,封面 COVER,碟片 」）、中间层嵌图（默认「./placeholder-1.pn」）、强调色（默认「#00FF00」）、总时长(秒)（默认「6」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
