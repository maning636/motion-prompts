# 间距拨盘布局

> 分类：镜头动效 ｜ 时长：4.3s ｜ 在线预览：https://maning789link.top/station.html

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】间距拨盘布局
【分类】镜头动效
【用途】框选描边+8手柄包住一排导航块，徽章数字逐格跳动、块被 gap 参数同帧推开再弹簧过冲回位，参数驱动布局可视化
【标签】参数 / 布局 / 设计工具
【画幅】1920×1080（16:9 横屏）
【时长】4.3 秒，动画确定性、首尾可循环

【视觉与设计规范】
框选描边+8手柄包住一排导航块，徽章数字逐格跳动、块被 gap 参数同帧推开再弹簧过冲回位，参数驱动布局可视化。
【可改文案】导航块1（默认「模板库」）、导航块2（默认「渲染队列」）、导航块3（默认「导演工作台」）、导航块4（默认「素材文档」）、导航块5（默认「发布设置」）、强调色（默认「#00FF00」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
