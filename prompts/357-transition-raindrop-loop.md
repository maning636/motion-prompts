# 多图雨滴连续轮换（模式特效）

> 分类：转场 ｜ 时长：9.3s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】多图雨滴连续轮换（模式特效）
【分类】转场
【用途】多图雨滴连续轮换：4 张图各配不同落点，竖椭圆落下→触地收圆→羽化扩散铺满，一条 GSAP 时间线串起并回首图（对应 9.3s 四图版），右上 DROP 计数器同步。
【标签】转场 / 雨滴 / 连续轮换 / 蒙版扩散
【画幅】1920×1080（16:9 横屏）
【时长】9.3 秒，动画确定性、首尾可循环

【视觉与设计规范】
沿用 transition-raindrop-reveal 的 radial-gradient mask 三拍（DROP 0.5s → IMPACT 0.18s → SPREAD 1.35s，羽化 14→62，glint 高光一拍，blur 16→0/scale 1.06→1 落定）。5 层叠放 L0..L4（L4=图1 回环），上层初始 mask 悬于画面外；每滴 t0=0.2+k*dropDur，计数器在 applyMask 内同步（seek-safe）。末滴 9.1s 铺满后定格首图。
【可改文案】图1（首尾回环）（默认「./placeholder-1.pn」）、图2（默认「./placeholder-2.pn」）、图3（默认「./placeholder-3.pn」）、图4（默认「./placeholder-4.pn」）、落点数组（x,y 百分比分号分隔）（默认「62,38;30,64;70,66;」）、单滴时长(秒)（默认「2.3」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
