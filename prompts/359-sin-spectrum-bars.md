# 正弦频谱柱组件（模式特效）

> 分类：数据可视化 ｜ 时长：6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】正弦频谱柱组件（模式特效）
【分类】数据可视化
【用途】独立频谱柱组件：N 根圆头柱 scaleY=确定性周期函数 f(t+phase)，相位错开看似随机实则固定，渲染器跳到任意帧状态确定。可复用于任何有声音的场景。
【标签】频谱 / 音频可视化 / 正弦 / seek-safe
【画幅】1920×1080（16:9 横屏）
【时长】6 秒，动画确定性、首尾可循环

【视觉与设计规范】
深底 #070907 + 柱色变量（默认荧光绿，带同色 box-shadow glow）。柱 16×340 圆头 origin bottom，scaleY = 0.12+0.88×(0.5-0.5cos(2π·ph))，T=0.75+(i%5)*0.12、相位 (i*0.13)%1.1 错开；speed 变量乘 t。禁 CSS animation / Math.random，全部 master onUpdate。入场容器 y+opacity，演完定格跳动中帧。
【可改文案】柱数（默认「48」）、柱色（默认「#00FF00」）、速度倍率（默认「1」）、总时长(秒)（默认「6」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
