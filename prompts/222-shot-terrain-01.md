# 支流线束地景开场

> 分类：镜头动效 ｜ 时长：5.5s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】支流线束地景开场
【分类】镜头动效
【用途】暗场数据地景：支流 bezier 切向汇入唯一主干、虚构 ID 标签浮在中景线上，三层景深 + 相机低速视差飞越——无数工作流汇成一个产品
【标签】开场 / 数据 / 景深
【画幅】1920×1080（16:9 横屏）
【时长】5.5 秒，动画确定性、首尾可循环

【视觉与设计规范】
暗场数据地景：支流 bezier 切向汇入唯一主干、虚构 ID 标签浮在中景线上，三层景深 + 相机低速视差飞越——无数工作流汇成一个产品。
【可改文案】标签1（默认「APP-1843」）、标签2（默认「TASK-209」）、标签3（默认「BUG-4417」）、标签4（默认「PR-882」）、标签5（默认「SYNC-310」）、标签6（默认「DATA-077」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
