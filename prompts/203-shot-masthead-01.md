# 标题降格节标签

> 分类：镜头动效 ｜ 时长：3.5s ｜ 在线预览：https://maning789link.top/station.html

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】标题降格节标签
【分类】镜头动效
【用途】大标题居中站稳一拍，单次连续补间缩到 0.3x 飞左上角落成本节门牌，内容区同步生长，章节交接零空档
【标签】标题 / 章节 / 过渡
【画幅】1920×1080（16:9 横屏）
【时长】3.5 秒，动画确定性、首尾可循环

【视觉与设计规范】
大标题居中站稳一拍，单次连续补间缩到 0.3x 飞左上角落成本节门牌，内容区同步生长，章节交接零空档。
【可改文案】章节标题（默认「自动发布」）、内容块1（默认「配置一次发布时间」）、内容块2（默认「选择目标平台」）、内容块3（默认「查看发布状态」）、强调色（默认「#00FF00」）、背景色（默认「#0A0A0F」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
