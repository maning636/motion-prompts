# 视差滑动相册开场（模式特效）

> 分类：标题与开场 ｜ 时长：9.6s ｜ 在线预览：https://motion.maning789link.top

把下面整段提示词粘贴给任意 AI（Claude Code / Codex / 豆包 / DeepSeek……）即可生成同款动效。

---
你是一位顶级前端动效工程师。请用 HTML + CSS + GSAP（CDN 引入）实现下面这个视频动效，输出一个自包含的单文件 HTML。

【动效名称】视差滑动相册开场（模式特效）
【分类】标题与开场
【用途】左 560px 米白纸面目录栏（当前行 0.42→1 点亮），右侧相框 x+scale+blur 三联动 expo.out 滑入，同源模糊放大背景 1/5 位移 2 倍时长慢速跟随，4 图轮换。相册/作品集开场用。
【标签】视差 / 相册 / 开场 / 目录栏
【画幅】1920×1080（16:9 横屏）
【时长】9.6 秒，动画确定性、首尾可循环

【视觉与设计规范】
目录栏 560×1080 #f8f5ed + 36px 网格，x:-560 推入 expo.out 0.62s 后锁死。相框 1120×630：x:240→0 + scale 0.92→1 + blur 12→0 三联动 expo.out 0.78s；同源背景 blur 20px + 暗 wash rgba(7,9,10,.3)，x:46→0 + scale 1.18→1.1 sine.out 1.78s（1/5 位移、2 倍时长 = 视差）。切换点 1.8/4.1/6.4s（interval 2.3），目录行 0.42→1 点亮。
【可改文案】大标题（默认「JUST TALK TO IT」）、说明块文案（默认「左边一条固定目录；相框从右边滑入，先」）、目录项（逗号分隔，4 行）（默认「DESCRIBE IT,AI SCA」）、轮换图1（默认「./placeholder-1.pn」）、轮换图2（默认「./placeholder-2.pn」）、轮换图3（默认「./placeholder-3.pn」）

【技术要求】
1. 先画出动画最完整时刻的静态布局，再编写 GSAP 时间线；动画必须是确定性的（同一时刻刷新，画面一致）。
2. 文案、数字、颜色集中定义为 JS 常量放在文件顶部，方便二次修改。
3. 只输出 HTML 代码，不要解释，不要省略。
